name: BioplusTerrainIOS
options:
  bundleIdPrefix: com.dabbio
settings:
  base:
    PRODUCT_BUNDLE_IDENTIFIER: com.dabbio.bioplus-terrain-ios
    MARKETING_VERSION: 1.0.0
    CURRENT_PROJECT_VERSION: 1
    SWIFT_VERSION: 5.0
    IPHONEOS_DEPLOYMENT_TARGET: 16.0
targets:
  BioplusTerrainIOS:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: BioplusTerrainIOS
        excludes:
          - Resources/Info.plist
    info:
      path: BioplusTerrainIOS/Resources/Info.plist
    settings:
      base:
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: ""
